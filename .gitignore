# Ceipal integration — never commit real credentials or runtime files
careers-config.php
careers-cache.json
careers-errors.log
.local-tools/

# Form submission logs contain candidate/inquiry PII — never commit
server/careers-submissions.log
server/contact-submissions.log

# Mail/API credentials — never commit; see server/.env.example for the template
server/.env
server/.env.*
!server/.env.example
