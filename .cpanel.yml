---
deployment:
  tasks:
    - export DEPLOYPATH=/home4/globalso/public_html/newsite/
    - /bin/mkdir -p "$DEPLOYPATH"
    - /bin/cp index.html "$DEPLOYPATH"
    - /bin/cp -R css "$DEPLOYPATH"
    - /bin/cp -R html "$DEPLOYPATH"
    - /bin/cp -R images "$DEPLOYPATH"
    - /bin/cp -R js "$DEPLOYPATH"